# Enforce Kyle-only GrooveStats

Status: pending a separate configuration approval.

The owner authorized GrooveStats only for Kyle, but `elemwarr` and `Crios` already contain structurally valid API keys. Enabling Simply Love's global switch can make those profiles eligible too. Do not display, delete, or rewrite their keys implicitly. A future approved change should make recoverable copies, remove or archive those credentials, validate the INI files, and document the result without recording secret values.
