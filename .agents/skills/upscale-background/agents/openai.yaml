interface:
  display_name: "Upscale Background"
  short_description: "Restore static ITGMania song backgrounds"
  default_prompt: "Use $upscale-background to faithfully restore a static song background, show labeled Before and After previews with dimensions and aspect ratios, allow Install for one exact candidate, require Install A or Install B when several exist, and safely complete that exact approved install."
