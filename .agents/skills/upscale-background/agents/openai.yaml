interface:
  display_name: "Upscale Background"
  short_description: "Restore static ITGMania song backgrounds"
  default_prompt: "Use $upscale-background to faithfully restore a static song background, show labeled Before and After previews with dimensions and aspect ratios, ask for an explicit Install A or Install B choice, and safely complete that exact approved install."
