interface:
  display_name: "Upscale ITGMania Banner"
  short_description: "Restore and safely install song banners"
  default_prompt: "Use $upscale-banner to restore this banner, show exact full-resolution comparisons, and perform only a hash-bound, queue-serialized install after exact-preview approval."
